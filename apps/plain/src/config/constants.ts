export const PLAIN_URL = "http://localhost:3000" as const

// Timeout for cursor to show animating blobs around it
export const IDLE_CURSOR_TIMEOUT = 3000 as const

// SVG size for animated technology that i worked with section
export const SVG_SIZE = 30 as const

// Delay for shooting svg in second in animated techonology
export const SVG_DELAY_SHOT = 4 as const
